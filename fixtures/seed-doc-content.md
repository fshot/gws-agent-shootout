# Seed Document

This is the GWS Agent Shootout seed document, used to test read and edit operations across all agent configurations.

## Section One

The purpose of this document is to provide a known, structured artifact that every agent configuration can read and verify. The content is deliberately simple so that success criteria are unambiguous.

- First bullet point in section one
- Second bullet point in section one
- Third bullet point in section one

## Section Two

This section exists to test whether agents preserve existing content when appending. If an edit operation modifies this section or removes it, the test has failed.
