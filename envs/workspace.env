# GWS Agent Shootout — Workspace (business) account
# Loaded by direnv when working in workspace-related directories

export GWS_ACCOUNT=fshotwell@cruxcapacity.com
export GWS_TARGET_ACCOUNT=fshotwell@gmail.com
export GWS_ACCOUNT_LABEL=workspace

export GCP_PROJECT=
export GOOGLE_CLIENT_CREDENTIALS="${HOME}/.config/gws/credentials-workspace.json"
export GOOGLE_TOKEN_PATH="${HOME}/.config/gws/token-workspace.json"

export GWS_TEST_CALENDAR="GWS Shootout"
