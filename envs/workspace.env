# GWS Agent Shootout — Workspace (business) account
# Loaded by direnv when working in workspace-related directories

GWS_ACCOUNT=fshotwell@cruxcapacity.com
GWS_TARGET_ACCOUNT=fshotwell@gmail.com
GWS_ACCOUNT_LABEL=workspace

GCP_PROJECT=
GOOGLE_CLIENT_CREDENTIALS="${HOME}/.config/gws/credentials-workspace.json"
GOOGLE_TOKEN_PATH="${HOME}/.config/gws/token-workspace.json"

GWS_TEST_CALENDAR="GWS Shootout"
